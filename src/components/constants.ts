export const BUTTONS_DATA = [...new Array(1000)].map((_, i) => `Button ${i + 1}`);
export const TEXT_FIELD_DATA = [...new Array(1000)].map((_, i) => `TextField ${i + 1}`);
export const CHECKBOX_DATA = [...new Array(1000)].map((_, i) => `Checkbox ${i + 1}`);
